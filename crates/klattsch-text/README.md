# klattsch-text

Parser and schedule compiler for `klattsch-rs` phoneme strings.

`klattsch-text` tokenizes `.klatt` input, normalizes text, and compiles phoneme
strings into schedules consumable by `klattsch-core`.

See the workspace README for examples and release notes:
https://github.com/tgies/klattsch-rs
