# klattsch-wav

RIFF/WAVE encoder for `klattsch-rs` render output.

`klattsch-wav` writes mono PCM WAV data and provides helpers for peak
normalization and metadata chunks.

See the workspace README for examples and release notes:
https://github.com/tgies/klattsch-rs
